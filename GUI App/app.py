import tkinter as tk #for GUI
from tkinter import filedialog, Text #pick the apps 
import os 

root = tk.Tk() 

#attach canvas to root
canvas = tk.Canvas(root, height=700, width=700, bg=#000000) #canvas makes the GUI bigger, 



root.mainloop() #similar to html, th